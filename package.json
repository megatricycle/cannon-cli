{
  "name": "cannon-cli",
  "version": "0.2.2",
  "description": "CLI for react-cannon boilerplate",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preclean": "rimraf src && mkdir src",
    "clean": "npm run clean:actions & npm run clean:components & npm run clean:reducers",
    "clean:actions": "mkdir src/actions",
    "clean:components": "mkdir src/components",
    "clean:reducers": "mkdir src/reducers"
  },
  "author": "Peter Rupa (pbmrupa@gmail.com)",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "change-case": "^2.3.1",
    "fs-promise": "^0.5.0",
    "yargs": "^4.7.1"
  },
  "devDependencies": {
    "rimraf": "^2.5.2"
  },
  "bin": {
    "cannon": "index.js"
  },
  "preferGlobal": "true"
}
